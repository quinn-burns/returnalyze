import ReturnsDriversContent from "@/components/returns-drivers/ReturnsDriversContent";

export const metadata = {
  title: "Returns Drivers — Return App",
};

export default function ReturnsDriversPage() {
  return <ReturnsDriversContent />;
}
